apiVersion: v1
kind: Service
metadata:
  name: unicx-backend-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: unicx-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000